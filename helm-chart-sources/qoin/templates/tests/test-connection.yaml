apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "qoin.fullname" . }}-test-connection"
  labels:
    {{- include "qoin.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "qoin.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
